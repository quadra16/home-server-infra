## Immich database credentials (copy to env/immich.env and secure this file)
# Example values; replace with strong, unique credentials
IMMICH_DB_USER=immich_user
IMMICH_DB_PASSWORD=change_this_to_a_strong_password
IMMICH_DB_NAME=immich_db

# If you store this file in version control, ensure secrets are removed.
